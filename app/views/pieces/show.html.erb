<h1 class="text-center"><%= @piece.game.name %></h1>
<br/>
<table class="board">
  <% 8.times do |row| %>
    <%= '<tr>'.html_safe %>
    <% 8.times do |col| %>
      <%= '<td>'.html_safe + content_tag(:span, selected_piece_present?(@game_pieces, row, col), class: ["piece", ("selected" if selected?(@piece, @game_pieces, row, col))]) + '</td>'.html_safe %>
    <% end %> 
  <% end %>
</table>
<br>
